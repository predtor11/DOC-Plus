#############################################
# Doc+ Environment Variables (Example)
# Copy to .env and fill real values (never commit secrets)
#############################################

# --- Supabase ---
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your_supabase_anon_or_publishable_key

# (Legacy / optional) If code references project id separately
VITE_SUPABASE_PROJECT_ID=your_project_id_here

# --- Clerk Auth ---
VITE_CLERK_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxxxxxxx

# --- AI (OpenRouter) ---
# If this key is sensitive in production, proxy it server-side instead of exposing.
VITE_OPENROUTER_API_KEY=sk_your_openrouter_api_key
VITE_OPENROUTER_MODEL=openai/gpt-3.5-turbo

# (Optional) Local model endpoint experimentation
# VITE_LM_STUDIO_URL=http://127.0.0.1:1234

# --- Email / Edge Functions ---
RESEND_API_KEY=re_your_resend_api_key
SITE_URL=http://localhost:5173
# SITE_URL=https://your-production-domain

# Add any custom variables below
# VITE_FEATURE_FLAG_X=true

#############################################
# Security Tips
# - Never commit real API keys
# - Rotate keys if exposed
# - Avoid putting powerful secrets in VITE_ vars
#############################################
